---
type: ds-component
env: 02-design-system
status: synced
figma-node: "227:16241"
updated: 2026-09-13
---

# Menu (SideBar TOC)

В Figma компонент называется **Menu** (`227:16241`). Локальный файл и класс `.ds-side-nav` сохранены как SideBar TOC. **Не путать** с профильным **Sidebar** `158:11468` и с **MenuMobile** `245:17643`.

## Назначение
Десктопная колонка оглавления: список **TitleSidebar** + финальный пункт «Контакты». Витрина **262×212**. На mobile/tablet — **MenuMobile**.

## Анатомия
Колонка (padding px 48 / py 24, gap 16) + N× TitleSidebar + text «Контакты» (H2 Gray_text).

## Варианты
| Вариант | node-id | Когда |
|---------|---------|-------|
| default | `227:16241` | единственный на витрине · 262×212 |

## Состояния
default; пункты — через State TitleSidebar (default = текущий / hover = остальные).

## Токены
| Свойство | Токен / значение |
|----------|------------------|
| size | 262×212 |
| padding | 48 горизонталь · 24 вертикаль |
| gap | 16 |
| item | TitleSidebar · H2 |
| «Контакты» | H2 · Gray_text `#888888` |

## Правила применения
Оглавление кейса / статьи. Профильная панель с аватаром и фильтрами — **Sidebar**.

## Чем не является
Не **Sidebar** профиля (`158:11468`), не Header, не Card, не **MenuMobile** (`245:17643`).
