# Каталог: tests/

## Назначение
Автопроверки продукта `ds-showcase/` и каркаса поставки Portfolio/Storybook (файловая структура, CSS-контракт / shared linkage без форка, shell витрины, page shell NFR, smoke entrypoint, token layer, Foundations catalog, Asset Store, Icons/Atomic, Composite Card/Sidebar, README витрины + корневой README поставки, Content Map / Scene Layout, Scene Renderer, CameraController math contract, Input Gesture Layer, Interactive Hits, Portfolio E2E layout/camera/interactions/viewport + Vite no-mock, Storybook inventory + main.js glob, smoke камеры/entry / Storybook+portfolio entrypoints).

## Файлы

### test_content_map_layout.mjs
- `TC-E2E-01` — `profile.role` / `card.title` из content-package
- `TC-E2E-02` — 7 слотов layout + zIndex paint order (about вместо comp/stiker)
- `TC-E2E-03` — `resolveAsset('avatar')` → путь под `ds-showcase/assets/`
- `TC-UNIT-01` — `computeContentAABB` по card+about; tapper вне AABB; FIXED_CHROME = sidebar+tapper
- `TC-UNIT-02` — `chipVariants` active/default
- `TC-UNIT-03` — `contact.*` без URL

### test_scene_renderer.mjs
- `TC-E2E-01` — world: bg, 3 card, about, tapper; chrome: sidebar only
- `TC-E2E-02` — геометрия Sidebar stretch / Card 310×310 / Tapper 40×104 portrait
- `TC-E2E-03` — `profile.role` / `card.title` из contentMap
- `TC-E2E-04` — три Card с одним InnoDragon-набором
- `TC-UNIT-01` — DOM-порядок по возрастанию zIndex
- `TC-UNIT-02` — contacts `href="#"` без http URL
- Доп.: contentAABB; camera.apply не пересоздаёт DOM

### test_portfolio_scene_layout.mjs
- `TC-E2E-01` — 1024×609 → `{1,0,0}`; слоты vs layout ±4px; Sidebar/Card/Tapper sizes; about cluster + stiker inside
- `TC-E2E-02` — 1366×768; sidebar chrome stretch; tapper right chrome 24px / 40×104
### test_portfolio_camera_e2e.mjs
- `TC-E2E-02` — zoom/pan только transform; identity children
- `TC-E2E-03` — pivot cursor vs center; Tapper zoom center
- `TC-E2E-04` — 100% keepWorldCenter; fitToContent AABB

### test_portfolio_interactions.mjs
- `TC-E2E-06` — broken image слот + console.warn spy
- `TC-E2E-07` — Card hover; contacts focusable no-nav; suppressClicks; Tapper a11y

### test_portfolio_viewport_branches.mjs
- `TC-E2E-05` — wide `{1,0,0}` vs narrow fit; pan/zoom после fit
- No-mock Vite HTTP smoke `portfolio/index.html` + `main.js` + tokens.css

### test_portfolio_scaffold.mjs
- `TC-E2E-01` — пути архитектуры §3.4 на диске (`shared/*`, `portfolio/js/*`, `.storybook/main.js`, `stories/`)
- `TC-E2E-04` — npm scripts `storybook` и `portfolio:dev`

### test_portfolio_entry_smoke.mjs
- `TC-E2E-02` — no-mock parse `portfolio/index.html`: DS CSS links, `.viewport`/`.world`, module `main.js`

### test_camera_stub.mjs
- `TC-E2E-03` — стартовый getState `{1,0,0}`; после zoom scale меняется; `apply` = translate…scale (typeof-only / forever-stub ожидания удалены)

### test_camera_controller.mjs
- `TC-E2E-01` — zoomBy + step; children world — те же node refs
- `TC-E2E-02` — zoomTo(1, keepWorldCenter) сохраняет world-центр
- `TC-E2E-03` — fitToContent(24) стабилен при повторе
- `TC-E2E-04` — pan за expand AABB → soft clamp / no-op по оси
- `TC-UNIT-01…04` — round-trip, pivot cursor, maxScale no-op, порядок translate→scale

### test_camera_math.mjs
- `TC-E2E-01` — цепочка zoom → pan → 100% → fit; clamp + children invariant
- `TC-UNIT-01…05` — screen↔world, pivot center/cursor, keepWorldCenter, fitToContent(24)
- `TC-UNIT-Clamp` — pan за expand AABB → no-op по оси
- `TC-UNIT-Fit-narrow` — viewport <1024 → fit уменьшает scale
- `TC-UNIT-MinMax` — zoom at min/max no-op
- `TC-UNIT-Apply` — transform-only; children left/top не трогаются
- AABB фикстура из `shared/layout.js` `computeContentAABB`; viewport 1024×609

### test_input_gestures.mjs
- `TC-E2E-01` — Ctrl+= → scale растёт
- `TC-E2E-02` — wheel без ctrl → pan translate, scale прежний; preventDefault
- `TC-E2E-03` — Ctrl+wheel → zoom + preventDefault
- `TC-E2E-04` — Space+drag → pan; suppressClicks / grab→grabbing на время жеста
- `TC-UNIT-01` — Ctrl/Shift+0 → zoomTo(1, keepWorldCenter); Shift+1 → fitToContent(24)
- `TC-UNIT-02` — bare `+` не зумит при фокусе в input
- `TC-UNIT-03` — Meta и Ctrl симметричны для zoom wheel (+ keyboard Cmd/Ctrl+=)

### test_input_routing.mjs
- `TC-E2E-01` — таблица хоткеев: in/out/100%/fit → zoomBy/zoomTo/fitToContent
- `TC-UNIT-Wheel-pan` — wheel без mod → pan; scale прежний
- `TC-UNIT-Wheel-zoom` — Ctrl/Meta+wheel → zoom к курсору; preventDefault
- `TC-UNIT-Space` — Space+drag → pan + suppressClicks
- `TC-UNIT-Edit` — bare +/- игнорируются в input/textarea

### test_interactions.mjs
- `TC-E2E-01` — Tapper plus → scale растёт (pivot viewportCenter)
- `TC-E2E-02` — Card hover-отличие (класс `ds-card` + правило `.ds-card:hover` / `--shadow`)
- `TC-E2E-03` — click contact → preventDefault; без внешнего URL
- `TC-E2E-04` — `suppressClicks` блокирует action на Card; после сброса hit-testing снова работает
- `TC-E2E-05` — error на card img → слот не схлопывается; getState камеры без изменений
- `TC-UNIT-01` — aria-label Tapper из `tapper.zoom_out` / `tapper.zoom_in`
- `TC-UNIT-02` — `onImageError` не бросает; логирует warn/error
- Доп.: после снятия suppressClicks Tapper снова зумит

### test_storybook_inventory.mjs
- `TC-E2E-01` — файлы story groups Chip…Media без placeholder stub
- `TC-E2E-02` — Chip Default/Active; Card Default/Hover (+ классы)
- `TC-E2E-03` — `.storybook/main.js` glob резолвит stories + `node --check` синтаксис
- Media assets / Sidebar contentMap / no local content dictionary — регресс

### test_ds_css_shared_linkage.mjs
- Linkage: `portfolio/index.html` + `.storybook/preview.js` → только `ds-showcase/css/*`
- `TC-E2E-02` — нет второго `tokens.css` / `components.css` вне `ds-showcase/css`
- `TC-UNIT-01` — `--color-black: #232323`
- Parity ±2px: Tapper 104×40, Card 310×310; stories используют `ds-*` классы

### test_portfolio_stubs.mjs
- `TC-E2E-01` — `node --check` + import; стартовый getState `{1,0,0}`; zoom обновляет scale
- `TC-UNIT-01` — поля getState (scale, translateX, translateY)
- `TC-UNIT-02` — `resolveAsset('avatar')` non-throwing string
- Проверки `package.json` scripts и каталогов `shared/` / `portfolio/` / `stories/` / `.storybook/`

### test_portfolio_entry.mjs
- `TC-E2E-01` — `portfolio/index.html` ссылается на `ds-showcase` tokens/components CSS
- `TC-E2E-02` — `main.js` вызывает mountScene / apply / getState / fitToContent(24); stub transform scale(1)
- `TC-E2E-03` — story-файлы Chip/Card/Sidebar (+ полный инвентарь групп)
- `TC-UNIT-01` — `preview.js` импортирует оба DS CSS

### test_ds_showcase_scaffold.mjs
- `TC-UNIT-01` — пути архитектуры §3.4, селекторы components/showcase, имена токенов
- `TC-E2E-01` — парсинг `index.html`, разрешение трёх CSS link, HTTP smoke без 404

### test_ds_showcase_shell.mjs
- `TC-E2E-01` — порядок секций IA, Chip/Link/Card варианты, Sidebar Skills/Contacts, демо-строки, Inter+fallback
- `TC-UNIT-01` — литералы content-package, 6 свотчей, media-слоты
- Регрессия TC-E2E-01 из задачи 1.1

### test_ds_showcase_smoke.mjs
- `TC-UNIT-01` — обязательные файлы продукта (`index.html`, три CSS)
- `TC-E2E-01` — no-mock HTTP smoke: 5 секций, слоты Avatar / Chip×2 / Link×2 / Card×2 / Sidebar / Icons×4 / Media×5
- `TC-E2E-02` — запрет tracked-изменений под `design/`
- Регрессия DemoContent (литералы content-package)

### test_ds_showcase_tokens.mjs
- `TC-UNIT-01` — сверка ColorToken + GrayL + Shadow + TypographyStyle с ТЗ §3.1; запрет токена «вв»
- `TC-E2E-01` — HTTP-доставка `tokens.css`; 7 цветов и `--shadow` канонические; Black `#232323`
- Регрессия no-mock smoke 1.3 (страница открывается, 5 секций)

### test_ds_showcase_foundations.mjs
- `TC-UNIT-01` — ровно 6 `.ds-swatch` в Foundations; нет свотча GrayL
- `TC-E2E-01` — имена + hex §3.1; фон свотчей через `var(--color-*)`; 80×80; type specimens на `var(--type-*-*)`; светлый `--page-bg`; HTTP smoke Foundations
- Регрессия: smoke 1.3 (5 секций); ColorToken hex из 2.1

### test_ds_showcase_assets.mjs
- `TC-UNIT-01` — MediaAsset keys ↔ файлы на диске; `src` в `index.html`; media boxes
- `TC-E2E-01` — HTTP 200 для index и всех ассетов Asset Store
- `TC-E2E-02` — ветка A2: временное отсутствие файла → 404 ассета, `.ds-placeholder` пропорции, страница usable
- Регрессия: smoke inventory + foundations tokens

### test_ds_showcase_atomic.mjs
- `TC-UNIT-01` — модификаторы Chip/Link в DOM Atomic
- `TC-UNIT-02` — `aria-label` у Tapper/Icons/Hover; Minus+Plus в Tapper
- `TC-E2E-01` — размеры Icons/Atomic ±2px; fill-токены; HTTP ассеты
- `TC-E2E-02` — static default/active/hover без `:hover` (UC-02 A1)
- Регрессия: tokens 2.1 + smoke section order

### test_ds_showcase_composite.mjs
- `TC-UNIT-01` — Card default без drop-shadow; hover/`--hover` = `var(--shadow)` (не «вв»)
- `TC-UNIT-02` — Contacts через `var(--type-caption-*)` + focus-visible
- `TC-E2E-01` — Card 310×310, тексты InnoDragon, static + `:hover`
- `TC-E2E-02` — Sidebar 310×561, Skills matrix/gap, bio, Caption contacts
- `TC-E2E-03` — Media×5 пропорции Figma/screen-spec
- Регрессия: Chip atomic, tokens Shadow, smoke IA

### test_ds_showcase_page.mjs
- `TC-UNIT-01` — `main` + ≥5 `section` + иерархия заголовков
- `TC-E2E-01` — ≥1280 без критичного horizontal scroll (CSS contract) + светлый `--page-bg`
- `TC-E2E-02` — узкий viewport: wrap / vertical stack
- `TC-E2E-03` — A1: Inter 400/500/600 + sans-serif; type tokens сохраняют иерархию
- `TC-E2E-04` — A2: битый ассет → 404 + `.ds-placeholder`; остальные секции доступны
- `TC-E2E-05` — `:focus-visible` + aria-label / alt на Link / Hover / Tapper / Contacts / Icons
- Регрессия: Atomic+Composite варианты + smoke IA

### test_ds_showcase_readme.mjs
- `TC-UNIT-01` — README не заглушка; нет секретов / `.cursor/mcp.json`
- `TC-E2E-01` — по README открывается `index.html` (file:// + HTTP); 5 секций IA
- `TC-E2E-02` — Figma `fileKey`/`node-id` и локальный evidence PNG резолвятся

### test_delivery_readme.mjs
- `TC-UNIT-01` — корневой README: Node LTS, npm ci/install, ds-showcase css/assets, scripts, `@ds-assets`, hotkeys, запреты, Figma `41:1416`; наличие `portfolio/README.md`
- `TC-E2E-01` — точные имена `npm run storybook` и `npm run portfolio:dev`
- `TC-E2E-02` — `package.json` scripts совпадают с README; alias в vite + Storybook main
- `TC-E2E-03` — no-mock smoke: Vite отдаёт Главную; Storybook `iframe.html` без crash окружения

Запуск: `npm test` (или `node --test` по списку scripts.test в package.json)

## Зависимости
- Node.js (встроенные `node:test`, `node:assert`, `node:http`)
- Файлы `ds-showcase/`
